name: require-approval-for-restarts
type: GlobalHook
tags: []
properties:
  eventType: PreToolUse
  activationMode: always
  hook:
    type: prompt
    prompt: If this action will restart or scale a resource, require human approval
      before proceeding.
    matcher: ^(restart_|scale_).*
    timeout: 30
